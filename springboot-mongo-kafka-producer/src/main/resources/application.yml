server:
  port: 8191
spring:
  data:
    mongodb:
      host: localhost
      port: 27017
      database: kafkamongodb
      repositories:
        enabled: true
logging:
  level:
    org:
      springframework:
        data:
          mongodb:
            core:
              MongoTemplate: DEBUG